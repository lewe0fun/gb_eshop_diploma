spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eshop
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
  generate-ddl: true
#  flyway:
#    baselineOnMigrate: true
#    baseline-version: 0
#место откуда загружать изображения
upload:
  #path: /home/iopa/uploads
  path: E:\uploads