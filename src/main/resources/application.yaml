spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eshop
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
  generate-ddl: true

upload:
  #path: /home/iopa/uploads
  path: E:\uploads

  # Категории товаров:
category:
  1: "baths"
  2: "sanitary ware"
  3: "mixers"
  4: "shower stalls"
  5: "kitchen sinks"
  6: "fittings"